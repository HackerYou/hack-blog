<%= form_tag '/posts' do %>

<% end %>


1. Add a 'new' action to your controller
2. Render new.html.erb
3. Fill in the example form above
4. Add a 'create' action to your controller
   - Post.create(title: params[:title], ... )

5. Redirect to index in the create action like this:

   redirect_to '/posts'

Commit to git all the time! Test your changes one at a time!